<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li><%= image_tag user.image_url %></li>
    <li><%= link_to user.username, user %></li>
    <!-- Add for all properties of a user --> 
    
  <% end %>
</ul>