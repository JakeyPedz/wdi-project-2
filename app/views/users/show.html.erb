<h1><%= @user.username %></h1>

<%= image_tag @user.image_url %>

<% @user.stories.each do |story| %>
  <%= link_to story.title, story %>
  
  <%= story.content %>
  <br>
  <%= link_to story.category %>
  <br>
  <%= link_to story.tagline %>
  <br>
<% end %>