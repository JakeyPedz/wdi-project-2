<h1><%= @user.username %></h1>

<% if @user.image? %>
<li><%= link_to (image_tag @user.image.url) %></li>
<% else %>
<li><%= link_to (image_tag @user.image_link) %></li>
<% end %>

<% @user.stories.each do |story| %>
  <%= link_to story.title, story %>
  
  <%= story.content %>
  
<% end %>
