<h1><%= @user.username %></h1>

<li><%= link_to (image_tag @user.image.thumb.url) %></li>

<ul>
  <li>
<% @user.stories.each do |story| %>
  <%= link_to story.title, story %>
  </li>
  <li>
  <%= story.content %>
  </li>
</ul>
  
<% end %>
